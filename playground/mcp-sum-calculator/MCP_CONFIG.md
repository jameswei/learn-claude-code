# Claude Desktop MCP Configuration

# To use this MCP server with Claude Desktop, add the following to ~/.claude/mcp.json

{
  "mcpServers": {
    "mcp-sum-calculator": {
      "command": "python3",
      "args": ["/Users/wjia/Workspaces/Personal/learn-claude-code/playground/mcp-sum-calculator/server.py"]
    }
  }
}